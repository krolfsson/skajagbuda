import { ExampleReport } from "@/components/ExampleReport";

/** Homepage example — same layout as /exempel, with landing CTA. */
export function AnalysisPreview() {
  return <ExampleReport showFooterCta />;
}
